import { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.btstays.user",
  appName: "bt-stays",
  webDir: "dist",
  bundledWebRuntime: false,
};

export default config;
